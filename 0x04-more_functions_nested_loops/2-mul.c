#include "main.h"
/**
 * mul - multiple two integers and return the result.
 * @a: The first number.
 * @b: The second number.
 *
 * Return: The result of multiply a and b.
 */
int mul(int a, int b)
{
	return ((a + b));
}
